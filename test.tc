int num;
int x;

int pN(int c) { pv = c; putd(); }
int pC(int c) { pv = c; putc(); }
int pS(int c) { pv = c; puts(); }
int T4() { if (x == (num-1)) { x = num+1; } }

int Mil(int m, int c) {
	int x = m * (c * c);
	return x;
}

void main() {
	// putc(65);
	int c = 's';
	pC(c);
	x = 999;
	Mil(1000, 500);
	pN(x);
	num = x;
	while (x) { x--; }
	c = 'e';
	pC(c);
	pS(" - all done!");
	exit();
}
